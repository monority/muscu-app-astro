---

import { t, setLocale, defaultLocale } from '../../i18n';
import AppLayout from '../../layouts/AppLayout.astro';
import PageHeader from '../../components/layout/PageHeader.astro';
import Card from '../../components/ui/Card.astro';
import CardHeader from '../../components/ui/CardHeader.astro';
import CardTitle from '../../components/ui/CardTitle.astro';
import CardContent from '../../components/ui/CardContent.astro';
setLocale(Astro.currentLocale ?? defaultLocale);
const L = t();
---

<AppLayout title={L.poids.title}>
  <PageHeader
    title={L.poids.title}
    description={L.poids.description}
  />

  <div
    class="poids"
    x-data="poidsApp()"
    x-init="init()"
    x-cloak
  >
    {/* ═══ Entry form ═══ */}
    <Card>
      <CardHeader>
        <CardTitle>{L.poids.newMeasure}</CardTitle>
      </CardHeader>
      <CardContent>
        <form class="poids__form" @submit.prevent="addEntry()">
          <div class="poids__grid">
            <div class="poids__field">
              <label for="poids-date" class="poids__label">{L.poids.date}</label>
              <input
                id="poids-date"
                type="date"
                class="poids__input"
                x-model="form.date"
                required
              />
            </div>
            <div class="poids__field">
              <label for="poids-weight" class="poids__label">
                {L.poids.weightLabel}
              </label>
              <input
                id="poids-weight"
                type="number"
                class="poids__input"
                step="0.1"
                min="20"
                max="300"
                inputmode="decimal"
                x-model.number="form.weight"
                placeholder={L.poids.weightPlaceholder}
                required
              />
            </div>
            <div class="poids__field">
              <label for="poids-arm" class="poids__label">Bras (cm)</label>
              <input
                id="poids-arm"
                type="number"
                class="poids__input"
                step="0.1"
                min="0"
                inputmode="decimal"
                x-model.number="form.arm"
                placeholder="—"
              />
            </div>
            <div class="poids__field">
              <label for="poids-chest" class="poids__label">Poitrine (cm)</label>
              <input
                id="poids-chest"
                type="number"
                class="poids__input"
                step="0.1"
                min="0"
                inputmode="decimal"
                x-model.number="form.chest"
                placeholder="—"
              />
            </div>
            <div class="poids__field">
              <label for="poids-waist" class="poids__label">Taille (cm)</label>
              <input
                id="poids-waist"
                type="number"
                class="poids__input"
                step="0.1"
                min="0"
                inputmode="decimal"
                x-model.number="form.waist"
                placeholder="—"
              />
            </div>
            <div class="poids__field">
              <label for="poids-hips" class="poids__label">Hanches (cm)</label>
              <input
                id="poids-hips"
                type="number"
                class="poids__input"
                step="0.1"
                min="0"
                inputmode="decimal"
                x-model.number="form.hips"
                placeholder="—"
              />
            </div>
          </div>

          <p class="poids__error" x-show="formError" x-text="formError" x-cloak x-transition.opacity.duration.180ms></p>
          <p class="poids__success" x-show="success" x-text="success" x-cloak x-transition.opacity.duration.180ms></p>

          <div class="poids__actions">
            <button
              type="button"
              class="poids__btn poids__btn--secondary"
              @click="resetForm()"
            >
              {L.common.reset}
            </button>
            <button type="submit" class="poids__btn poids__btn--primary">
              {L.poids.save}
            </button>
          </div>
        </form>
      </CardContent>
    </Card>

    {/* ═══ Stats grid ═══ */}
    <template x-if="records.length > 0">
      <div class="poids__stats">
        <div class="poids__stat poids__stat--hero">
          <span class="poids__stat-label">Poids actuel</span>
          <div class="poids__stat-value-row">
            <span class="poids__stat-value" x-text="formatNumber(latest?.weight)"></span>
            <span class="poids__stat-unit">{L.common.kg}</span>
          </div>
          <span
            class="poids__stat-delta"
            :class="weightDelta < 0 ? 'is-down' : weightDelta > 0 ? 'is-up' : ''"
            x-text="formatDelta(weightDelta, 'kg')"
          ></span>
        </div>

        <div class="poids__stat" x-show="latest?.arm != null">
          <span class="poids__stat-label">Bras</span>
          <div class="poids__stat-value-row">
            <span class="poids__stat-value" x-text="formatNumber(latest?.arm)"></span>
            <span class="poids__stat-unit">cm</span>
          </div>
          <span
            class="poids__stat-delta"
            :class="armDelta < 0 ? 'is-down' : armDelta > 0 ? 'is-up' : ''"
            x-text="formatDelta(armDelta, 'cm')"
          ></span>
        </div>

        <div class="poids__stat" x-show="latest?.chest != null">
          <span class="poids__stat-label">Poitrine</span>
          <div class="poids__stat-value-row">
            <span class="poids__stat-value" x-text="formatNumber(latest?.chest)"></span>
            <span class="poids__stat-unit">cm</span>
          </div>
          <span
            class="poids__stat-delta"
            :class="chestDelta < 0 ? 'is-down' : chestDelta > 0 ? 'is-up' : ''"
            x-text="formatDelta(chestDelta, 'cm')"
          ></span>
        </div>

        <div class="poids__stat" x-show="latest?.waist != null">
          <span class="poids__stat-label">Taille</span>
          <div class="poids__stat-value-row">
            <span class="poids__stat-value" x-text="formatNumber(latest?.waist)"></span>
            <span class="poids__stat-unit">cm</span>
          </div>
          <span
            class="poids__stat-delta"
            :class="waistDelta < 0 ? 'is-down' : waistDelta > 0 ? 'is-up' : ''"
            x-text="formatDelta(waistDelta, 'cm')"
          ></span>
        </div>

        <div class="poids__stat" x-show="latest?.hips != null">
          <span class="poids__stat-label">Hanches</span>
          <div class="poids__stat-value-row">
            <span class="poids__stat-value" x-text="formatNumber(latest?.hips)"></span>
            <span class="poids__stat-unit">cm</span>
          </div>
          <span
            class="poids__stat-delta"
            :class="hipsDelta < 0 ? 'is-down' : hipsDelta > 0 ? 'is-up' : ''"
            x-text="formatDelta(hipsDelta, 'cm')"
          ></span>
        </div>
      </div>
    </template>

    {/* ═══ Weight chart (SVG) ═══ */}
    <Card>
      <CardHeader>
        <div class="poids__chart-head">
          <CardTitle>{L.poids.chartTitle}</CardTitle>
          <span class="poids__chart-meta" x-show="records.length > 0">
            <span x-text="records.length"></span>
            {L.poids.entrySingular}<span x-show="records.length > 1">s</span>
          </span>
        </div>
      </CardHeader>
      <CardContent>
        <template x-if="records.length === 0">
          <p class="poids__empty">
            {L.poids.empty} {L.poids.addFirst}
          </p>
        </template>
        <template x-if="records.length > 0">
          <div class="poids__chart-wrap" x-html="renderChart()"></div>
        </template>
      </CardContent>
    </Card>

    {/* ═══ History table ═══ */}
    <template x-if="records.length > 0">
      <Card>
        <CardHeader>
          <CardTitle>{L.historique.titleShort}</CardTitle>
        </CardHeader>
        <CardContent>
          <div class="poids__table-wrap">
            <table class="poids__table">
              <thead>
                <tr>
                  <th scope="col">{L.poids.date}</th>
                  <th scope="col">Poids</th>
                  <th scope="col">Bras</th>
                  <th scope="col">Poitrine</th>
                  <th scope="col">Taille</th>
                  <th scope="col">Hanches</th>
                  <th scope="col" class="poids__th-end"></th>
                </tr>
              </thead>
              <tbody>
                <template x-for="(r, i) in [...records].reverse()" :key="i">
                  <tr>
                    <td x-text="formatDate(r.date)"></td>
                    <td class="poids__num" x-text="formatNumber(r.weight) + ' kg'"></td>
                    <td class="poids__num" x-text="r.arm != null ? formatNumber(r.arm) + ' cm' : '—'"></td>
                    <td class="poids__num" x-text="r.chest != null ? formatNumber(r.chest) + ' cm' : '—'"></td>
                    <td class="poids__num" x-text="r.waist != null ? formatNumber(r.waist) + ' cm' : '—'"></td>
                    <td class="poids__num" x-text="r.hips != null ? formatNumber(r.hips) + ' cm' : '—'"></td>
                    <td class="poids__td-end">
                      <button
                        type="button"
                        class="poids__icon-btn"
                        @click="removeEntry(r.date)"
                        :aria-label="'Supprimer la mesure du ' + formatDate(r.date)"
                        title={L.poids.delete}
                      >
                        <svg
                          xmlns="http://www.w3.org/2000/svg"
                          viewBox="0 0 24 24"
                          fill="none"
                          stroke="currentColor"
                          stroke-width="1.8"
                          stroke-linecap="round"
                          stroke-linejoin="round"
                          aria-hidden="true"
                        >
                          <polyline points="3 6 5 6 21 6"></polyline>
                          <path d="M19 6l-1 14a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2L5 6"></path>
                        </svg>
                      </button>
                    </td>
                  </tr>
                </template>
              </tbody>
            </table>
          </div>
        </CardContent>
      </Card>
    </template>
  </div>
</AppLayout>

<script>
  import {
    getBodyRecords,
    addBodyRecord,
    deleteBodyRecord,
    type BodyRecord,
  } from '../../lib/storage';

  import { t } from '../../i18n';

  const LABELS = t();

  interface BodyForm {
    date: string;
    weight: number | null;
    arm: number | null;
    chest: number | null;
    waist: number | null;
    hips: number | null;
  }

  function todayISO(): string {
    return new Date().toISOString().split('T')[0];
  }

  function fmtNum(n: number | null | undefined): string {
    if (n == null || !Number.isFinite(n)) return '—';
    return Math.round(n * 10) / 10 + '';
  }

  function fmtDate(iso: string): string {
    if (!iso) return '—';
    const d = new Date(iso);
    if (isNaN(d.getTime())) return iso;
    return d.toLocaleDateString('fr-FR', {
      day: '2-digit',
      month: 'short',
      year: 'numeric',
    });
  }

  function fmtDelta(delta: number | null, unit: string): string {
    if (delta == null || !Number.isFinite(delta) || delta === 0) {
      return '—';
    }
    const sign = delta > 0 ? '+' : '';
    return `${sign}${Math.round(delta * 10) / 10} ${unit} ${LABELS.poids.vsPrev}`;
  }

  document.addEventListener('alpine:init', () => {
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    const Alpine = (window as any).Alpine;

    Alpine.data('poidsApp', () => ({
      // ── State ──
      records: [] as BodyRecord[],
      form: {
        date: todayISO(),
        weight: null,
        arm: null,
        chest: null,
        waist: null,
        hips: null,
      } as BodyForm,
      formError: '',
      success: '',

      // ── Init ──
      init() {
        this.refresh();
      },

      // ── Data ──
      refresh() {
        this.records = getBodyRecords();
      },

      // ── Derived ──
      get latest(): BodyRecord | null {
        return this.records.length > 0 ? this.records[this.records.length - 1] : null;
      },
      get previous(): BodyRecord | null {
        return this.records.length > 1 ? this.records[this.records.length - 2] : null;
      },

      get weightDelta(): number | null {
        if (!this.latest || !this.previous) return null;
        return this.latest.weight - this.previous.weight;
      },
      get armDelta(): number | null {
        if (!this.latest || !this.previous) return null;
        if (this.latest.arm == null || this.previous.arm == null) return null;
        return this.latest.arm - this.previous.arm;
      },
      get chestDelta(): number | null {
        if (!this.latest || !this.previous) return null;
        if (this.latest.chest == null || this.previous.chest == null) return null;
        return this.latest.chest - this.previous.chest;
      },
      get waistDelta(): number | null {
        if (!this.latest || !this.previous) return null;
        if (this.latest.waist == null || this.previous.waist == null) return null;
        return this.latest.waist - this.previous.waist;
      },
      get hipsDelta(): number | null {
        if (!this.latest || !this.previous) return null;
        if (this.latest.hips == null || this.previous.hips == null) return null;
        return this.latest.hips - this.previous.hips;
      },

      // ── Actions ──
      resetForm() {
        this.form = {
          date: todayISO(),
          weight: null,
          arm: null,
          chest: null,
          waist: null,
          hips: null,
        };
        this.formError = '';
        this.success = '';
      },

      addEntry() {
        this.formError = '';
        this.success = '';

        if (!this.form.date) {
          this.formError = 'La date est obligatoire.';
          return;
        }
        if (
          this.form.weight == null ||
          !Number.isFinite(this.form.weight) ||
          this.form.weight <= 0
        ) {
          this.formError = LABELS.poids.weightInvalid;
          return;
        }
        if (this.form.weight < 20 || this.form.weight > 300) {
          this.formError = LABELS.poids.weightInvalid;
          return;
        }

        const record: BodyRecord = {
          date: this.form.date,
          weight: this.form.weight,
        };
        if (this.form.arm != null && Number.isFinite(this.form.arm) && this.form.arm > 0) {
          record.arm = this.form.arm;
        }
        if (this.form.chest != null && Number.isFinite(this.form.chest) && this.form.chest > 0) {
          record.chest = this.form.chest;
        }
        if (this.form.waist != null && Number.isFinite(this.form.waist) && this.form.waist > 0) {
          record.waist = this.form.waist;
        }
        if (this.form.hips != null && Number.isFinite(this.form.hips) && this.form.hips > 0) {
          record.hips = this.form.hips;
        }

        addBodyRecord(record);
        this.refresh();
        this.success = LABELS.poids.saved;
        this.form.weight = null;
        this.form.arm = null;
        this.form.chest = null;
        this.form.waist = null;
        this.form.hips = null;
        setTimeout(() => {
          if (this.success === LABELS.poids.saved) this.success = '';
        }, 3000);
      },

      removeEntry(date: string) {
        if (!window.confirm(LABELS.poids.deleteConfirm)) return;
        deleteBodyRecord(date);
        this.refresh();
      },

      // ── Display helpers ──
      formatNumber(n: number | null | undefined): string {
        return fmtNum(n);
      },
      formatDate(iso: string): string {
        return fmtDate(iso);
      },
      formatDelta(delta: number | null, unit: string): string {
        return fmtDelta(delta, unit);
      },

      /**
       * Renders an SVG line chart of the weight history. The chart is
       * rebuilt on every call so it always reflects the latest data.
       * Coordinates use a 0..1000 × 0..260 viewBox so it scales cleanly
       * to any container width via preserveAspectRatio.
       */
      renderChart(): string {
        if (this.records.length === 0) return '';
        const W = 1000;
        const H = 260;
        const PAD_L = 50;
        const PAD_R = 30;
        const PAD_T = 30;
        const PAD_B = 50;
        const plotW = W - PAD_L - PAD_R;
        const plotH = H - PAD_T - PAD_B;

        const weights = this.records.map((r) => r.weight);
        let minW = Math.min(...weights);
        let maxW = Math.max(...weights);
        // Add a small padding so the line never touches the chart edges.
        const span = maxW - minW;
        if (span < 1) {
          minW = minW - 0.5;
          maxW = maxW + 0.5;
        } else {
          minW = minW - span * 0.1;
          maxW = maxW + span * 0.1;
        }

        const xAt = (i: number) => {
          if (this.records.length === 1) return PAD_L + plotW / 2;
          return PAD_L + (i / (this.records.length - 1)) * plotW;
        };
        const yAt = (w: number) => {
          if (maxW === minW) return PAD_T + plotH / 2;
          return PAD_T + (1 - (w - minW) / (maxW - minW)) * plotH;
        };

        // Y-axis ticks (4 levels).
        const ticks: number[] = [];
        for (let k = 0; k <= 3; k++) {
          ticks.push(minW + ((maxW - minW) * k) / 3);
        }

        // Build the path.
        const path = this.records
          .map((r, i) => `${i === 0 ? 'M' : 'L'} ${xAt(i).toFixed(1)} ${yAt(r.weight).toFixed(1)}`)
          .join(' ');

        // First/last as accent points.
        const first = this.records[0];
        const last = this.records[this.records.length - 1];

        // X labels: show first, middle, last to avoid clutter.
        const labelIdxs: number[] = [];
        if (this.records.length === 1) {
          labelIdxs.push(0);
        } else {
          labelIdxs.push(0);
          if (this.records.length > 2) {
            labelIdxs.push(Math.floor((this.records.length - 1) / 2));
          }
          labelIdxs.push(this.records.length - 1);
        }

        let svg = `<svg viewBox="0 0 ${W} ${H}" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="xMidYMid meet" class="poids__chart" role="img" aria-label="${LABELS.poids.chartTitle}">`;

        // Background
        svg += `<rect x="0" y="0" width="${W}" height="${H}" fill="transparent"/>`;

        // Grid lines + Y labels
        for (const t of ticks) {
          const y = yAt(t);
          svg += `<line x1="${PAD_L}" y1="${y.toFixed(1)}" x2="${W - PAD_R}" y2="${y.toFixed(1)}" stroke="rgba(255,255,255,0.08)" stroke-width="1"/>`;
          svg += `<text x="${PAD_L - 8}" y="${(y + 4).toFixed(1)}" font-size="14" fill="#9CA3AF" text-anchor="end" font-family="var(--font-display, sans-serif)">${Math.round(t * 10) / 10}</text>`;
        }

        // Area under the line
        const areaPath =
          `M ${xAt(0).toFixed(1)} ${(H - PAD_B).toFixed(1)} ` +
          this.records
            .map((r, i) => `L ${xAt(i).toFixed(1)} ${yAt(r.weight).toFixed(1)}`)
            .join(' ') +
          ` L ${xAt(this.records.length - 1).toFixed(1)} ${(H - PAD_B).toFixed(1)} Z`;
        svg += `<defs><linearGradient id="poids-grad" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stop-color="#D97706" stop-opacity="0.35"/><stop offset="100%" stop-color="#D97706" stop-opacity="0"/></linearGradient></defs>`;
        svg += `<path d="${areaPath}" fill="url(#poids-grad)"/>`;

        // Line
        svg += `<path d="${path}" fill="none" stroke="#D97706" stroke-width="3" stroke-linejoin="round" stroke-linecap="round"/>`;

        // First / last accent dots
        svg += `<circle cx="${xAt(0).toFixed(1)}" cy="${yAt(first.weight).toFixed(1)}" r="5" fill="#D97706" stroke="#050505" stroke-width="2"/>`;
        svg += `<circle cx="${xAt(this.records.length - 1).toFixed(1)}" cy="${yAt(last.weight).toFixed(1)}" r="6" fill="#D97706" stroke="#050505" stroke-width="2"/>`;

        // X labels
        for (const i of labelIdxs) {
          const r = this.records[i];
          svg += `<text x="${xAt(i).toFixed(1)}" y="${(H - PAD_B + 24).toFixed(1)}" font-size="14" fill="#9CA3AF" text-anchor="middle" font-family="var(--font-body, sans-serif)">${fmtDate(r.date)}</text>`;
        }

        // Min / max labels on the right of first/last dots
        svg += `<text x="${(xAt(0) - 12).toFixed(1)}" y="${(yAt(first.weight) + 4).toFixed(1)}" font-size="14" font-weight="700" fill="#F6F6F6" text-anchor="end" font-family="var(--font-display, sans-serif)">${Math.round(first.weight * 10) / 10} kg</text>`;
        svg += `<text x="${(xAt(this.records.length - 1) + 12).toFixed(1)}" y="${(yAt(last.weight) + 4).toFixed(1)}" font-size="14" font-weight="700" fill="#F6F6F6" text-anchor="start" font-family="var(--font-display, sans-serif)">${Math.round(last.weight * 10) / 10} kg</text>`;

        svg += `</svg>`;
        return svg;
      },
    }));
  });
</script>

{/* Alpine.js CDN — MUST be AFTER the app script (type="module" runs before defer) */}
<script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.14.1/dist/cdn.min.js"></script>

<style>
  [x-cloak] {
    display: none !important;
  }

  .poids {
    max-width: 88rem;
    margin: 0 auto;
    padding-bottom: 6rem;
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  /* ═══ Form ═══ */
  .poids__form {
    display: flex;
    flex-direction: column;
    gap: 1.4rem;
  }

  .poids__grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.2rem;
  }

  @media (min-width: 640px) {
    .poids__grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  @media (min-width: 960px) {
    .poids__grid {
      grid-template-columns: repeat(6, 1fr);
    }
  }

  .poids__field {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
  }

  .poids__label {
    font-size: 1.2rem;
    font-weight: 500;
    color: var(--color-muted);
    line-height: 1.2;
  }

  .poids__req {
    color: var(--color-destructive);
    margin-left: 0.2rem;
  }

  .poids__input {
    width: 100%;
    height: 4.6rem;
    padding: 0 1.4rem;
    background-color: var(--color-surface-2);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    color: var(--color-text);
    font-family: var(--font-body);
    font-size: 1.4rem;
    outline: none;
    transition: border-color var(--transition-fast), box-shadow var(--transition-fast);
    -webkit-appearance: none;
    appearance: none;
  }

  .poids__input:focus {
    border-color: var(--color-accent);
    box-shadow: var(--focus-ring);
  }

  .poids__input[type='date'] {
    color-scheme: dark;
  }

  .poids__error {
    background-color: hsl(0 80% 55% / 0.10);
    border: 1px solid hsl(0 80% 55% / 0.30);
    border-radius: var(--radius-sm);
    color: var(--color-destructive);
    font-size: 1.2rem;
    font-weight: 500;
    padding: 0.8rem 1.2rem;
    margin: 0;
  }

  .poids__success {
    background-color: hsl(142 70% 45% / 0.10);
    border: 1px solid hsl(142 70% 45% / 0.30);
    border-radius: var(--radius-sm);
    color: var(--color-success);
    font-size: 1.2rem;
    font-weight: 500;
    padding: 0.8rem 1.2rem;
    margin: 0;
  }

  .poids__actions {
    display: flex;
    justify-content: flex-end;
    align-items: center;
    gap: 0.8rem;
    flex-wrap: wrap;
  }

  .poids__btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    height: 4.4rem;
    padding: 0 1.8rem;
    border-radius: var(--radius-md);
    border: 1px solid transparent;
    font-family: var(--font-body);
    font-size: 1.3rem;
    font-weight: 700;
    letter-spacing: 0.02em;
    cursor: pointer;
    min-width: 8rem;
    transition: background-color var(--transition-fast),
                border-color var(--transition-fast),
                color var(--transition-fast),
                transform var(--transition-fast);
  }

  .poids__btn:active {
    transform: scale(0.97);
  }

  .poids__btn--primary {
    background-color: var(--color-accent);
    color: var(--color-accent-foreground);
    border-color: var(--color-accent);
  }

  .poids__btn--primary:hover {
    background-color: var(--color-accent-hover);
    border-color: var(--color-accent-hover);
  }

  .poids__btn--secondary {
    background-color: transparent;
    color: var(--color-muted);
    border-color: var(--color-border);
  }

  .poids__btn--secondary:hover {
    background-color: var(--alpha-bg-subtle);
    color: var(--color-text);
    border-color: var(--alpha-border);
  }

  /* ═══ Stats grid ═══ */
  .poids__stats {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.2rem;
  }

  @media (min-width: 640px) {
    .poids__stats {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  @media (min-width: 960px) {
    .poids__stats {
      grid-template-columns: repeat(5, 1fr);
    }
  }

  .poids__stat {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
    padding: 1.6rem 1.8rem;
    background-color: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
  }

  .poids__stat--hero {
    grid-column: 1 / -1;
    background-color: hsl(32 95% 50% / 0.08);
    border-color: hsl(32 95% 50% / 0.30);
  }

  @media (min-width: 640px) {
    .poids__stat--hero {
      grid-column: auto;
    }
  }

  .poids__stat-label {
    font-size: 1.0rem;
    text-transform: uppercase;
    letter-spacing: 0.14em;
    color: var(--color-muted);
    font-weight: 700;
  }

  .poids__stat-value-row {
    display: flex;
    align-items: baseline;
    gap: 0.4rem;
  }

  .poids__stat-value {
    font-family: var(--font-display);
    font-size: 2.4rem;
    font-weight: 700;
    color: var(--color-text);
    font-variant-numeric: tabular-nums;
    line-height: 1.1;
  }

  .poids__stat--hero .poids__stat-value {
    font-size: 3rem;
    color: var(--color-accent);
  }

  .poids__stat-unit {
    font-family: var(--font-body);
    font-size: 1.3rem;
    color: var(--color-muted);
    font-weight: 600;
  }

  .poids__stat-delta {
    font-size: 1.1rem;
    color: var(--color-muted);
    font-weight: 500;
  }

  .poids__stat-delta.is-up {
    color: var(--color-accent);
  }

  .poids__stat-delta.is-down {
    color: var(--color-success);
  }

  /* ═══ Chart ═══ */
  .poids__chart-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .poids__chart-meta {
    font-size: 1.1rem;
    color: var(--color-muted);
    text-transform: uppercase;
    letter-spacing: 0.10em;
    font-weight: 700;
  }

  .poids__chart-wrap {
    width: 100%;
    overflow-x: auto;
  }

  .poids__chart {
    width: 100%;
    height: auto;
    min-height: 24rem;
    display: block;
  }

  .poids__empty {
    text-align: center;
    color: var(--color-muted);
    font-size: 1.3rem;
    padding: 2rem 1rem;
    margin: 0;
    line-height: 1.5;
  }

  /* ═══ History table ═══ */
  .poids__table-wrap {
    overflow-x: auto;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
  }

  .poids__table {
    width: 100%;
    border-collapse: collapse;
    font-size: 1.3rem;
  }

  .poids__table thead th {
    background-color: var(--color-surface-3);
    color: var(--color-muted);
    font-family: var(--font-display);
    font-size: 1.0rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.14em;
    text-align: left;
    padding: 1.0rem 1.4rem;
    border-bottom: 1px solid var(--color-border);
    white-space: nowrap;
  }

  .poids__th-end {
    text-align: right !important;
  }

  .poids__table tbody td {
    padding: 0.9rem 1.4rem;
    border-bottom: 1px solid var(--color-border);
    vertical-align: middle;
  }

  .poids__table tbody tr:last-child td {
    border-bottom: none;
  }

  .poids__table tbody tr:nth-child(even) {
    background-color: var(--color-surface-2);
  }

  .poids__num {
    font-family: var(--font-display);
    font-variant-numeric: tabular-nums;
    font-weight: 600;
    color: var(--color-text);
  }

  .poids__td-end {
    text-align: right;
  }

  .poids__icon-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 3.4rem;
    height: 3.4rem;
    background-color: transparent;
    border: 1px solid transparent;
    border-radius: var(--radius-sm);
    color: var(--color-muted);
    cursor: pointer;
    transition: background-color var(--transition-fast),
                border-color var(--transition-fast),
                color var(--transition-fast);
  }

  .poids__icon-btn:hover {
    background-color: hsl(0 80% 55% / 0.10);
    border-color: hsl(0 80% 55% / 0.30);
    color: var(--color-destructive);
  }

  .poids__icon-btn svg {
    width: 1.6rem;
    height: 1.6rem;
  }

  /* ═══ Reduced motion ═══ */
  @media (prefers-reduced-motion: reduce) {
    .poids__input,
    .poids__btn,
    .poids__icon-btn {
      transition: none;
      transform: none;
    }
  }
</style>
