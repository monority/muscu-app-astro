<div data-toast-container class="toast-container" role="status" aria-live="polite"></div>

<style>
  .toast-container {
    position: fixed;
    z-index: 1000;
    bottom: 2rem;
    right: 2rem;
    display: grid;
    gap: 0.8rem;
    pointer-events: none;
    max-width: 32rem;
    width: 100%;
  }

  @media (max-width: 639px) {
    .toast-container {
      left: 1.2rem;
      right: 1.2rem;
      bottom: calc(1.2rem + var(--nav-bar-height, 0px) + env(safe-area-inset-bottom, 0px));
      max-width: none;
    }
  }

  .toast {
    pointer-events: auto;
    padding: 1rem 1.4rem;
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    background: var(--surface);
    color: var(--text);
    font-size: 1.3rem;
    font-weight: 500;
    line-height: 1.4;
    opacity: 0;
    transform: translateY(1.2rem);
    transition: opacity 250ms ease, transform 250ms ease;
    cursor: pointer;
    -webkit-tap-highlight-color: transparent;
  }

  .toast--visible {
    opacity: 1;
    transform: translateY(0);
  }

  .toast--success {
    border-color: hsl(140 50% 40% / 0.35);
    background: hsl(140 50% 40% / 0.08);
  }

  .toast--error {
    border-color: hsl(24 100% 55% / 0.35);
    background: hsl(24 100% 55% / 0.08);
  }

  .toast--info {
    border-color: hsl(0 0% 100% / 0.1);
    background: hsl(0 0% 100% / 0.04);
  }
</style>
