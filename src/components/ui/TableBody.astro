---
/**
 * TableBody — <tbody> wrapper. Passes children through unchanged.
 */
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;

const classes = [className].filter(Boolean).join(' ');
---

<tbody class={classes}>
  <slot />
</tbody>
