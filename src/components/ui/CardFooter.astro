---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;

const classes = [
  'flex flex-row justify-end gap-2 pt-3 border-t border-[var(--color-border)]',
  className,
]
  .filter(Boolean)
  .join(' ');
---

<div class={classes}>
  <slot />
</div>
