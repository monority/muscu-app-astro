---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class:list={['grid grid-cols-2 gap-[1rem] sm:grid-cols-[repeat(auto-fit,minmax(16rem,1fr))]', className]}>
  <slot />
</div>
