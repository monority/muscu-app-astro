---
interface Props {
  width?: string;
  height?: string;
  borderRadius?: string;
  class?: string;
}

const { width = "100%", height = "1.6rem", borderRadius = "var(--radius-sm)", class: className } = Astro.props;
---

<div class={"skeleton" + (className ? " " + className : "")} style={{ width, height, borderRadius }} aria-hidden="true"></div>


