---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;

const classes = [
  'flex flex-col gap-[var(--spacing-2)] pb-[var(--spacing-3)]',
  className,
]
  .filter(Boolean)
  .join(' ');
---

<div class={classes}>
  <slot />
</div>
