---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;

const classes = ['flex flex-row justify-between items-center pb-3', className]
  .filter(Boolean)
  .join(' ');
---

<div class={classes}>
  <slot />
</div>
