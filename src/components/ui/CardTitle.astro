---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;

const classes = [
  'font-[var(--font-display)] text-[1.4rem] font-bold',
  className,
]
  .filter(Boolean)
  .join(' ');
---

<h3 class={classes}>
  <slot />
</h3>
